{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:css && npm run build:js",
    "build:css": "tailwindcss -i ./resources/css/sampaui.css -o ./dist/sampaui.css --minify",
    "build:js": "vite build"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "bootstrap-icons": "^1.11.3",
    "easymde": "^2.21.0"
  }
}
